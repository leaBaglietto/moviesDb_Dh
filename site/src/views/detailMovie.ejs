<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include('partials/head') %>
        <title>Ficha tecnica - <%=movie.title%></title>
            </head>

    <body>
        <!--HEADER-->
        <%- include('partials/header') %>
            <!-- FIN HEADER -->
            <div class="container_movieDetail">
                    <h4><%=movie.title%></h4>
                    <ul>
                        <li>
                            Duración: <%=movie.length%> 
                        </li>

                        <li>
                            Premios: <%=movie.awards%> 
                        </li>
                        
                        <li>
                            Rating: <%=movie.rating%> 
                        </li>
<!-- ACA PONER EL CODIGO QUE ESTA EN EL WORD -->
                    
                    </ul>
                </div>
                <a href="/movies/movieEdit/<%=movie.id%> ">
                <button>Modificar</button>
                </a>

                    <form action="/movies/movieDelete/<%=movie.id%>" method="POST">
                        <button type="submit">Eliminar</button>
                    </form>

            </div>
    </body>

</html>